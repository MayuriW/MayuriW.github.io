import { CustStruDirDirective } from './cust-stru-dir.directive';

describe('CustStruDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustStruDirDirective();
    expect(directive).toBeTruthy();
  });
});
